import React from "react";

const CTAButton = () => {
  return (
    <button className="text-white border border-primary px-4 font-bold py-1 rounded-xl hover:bg-primary hover:text-text duration-200 transition-colors cursor-pointer">
      Get Started
    </button>
  );
};

export default CTAButton;
